<table class="genres-sidebar table">
  <tr>
    <th>ジャンル検索</th>
  </tr>
  <% genres.each do |genre| %>
    <tr>
      <td>
        <%= link_to genre.name, search_path("value": genre.id) %>
      </td>
    </tr>
  <% end %>
</table>