<div class="container">
  <div class="row">
    <div class="col-2">
      <table class="genres-sidebar table">
        <tr>
          <th>ジャンル検索</th>
        </tr>
        <tr>
          <td>余裕があれば実装</td>
        </tr>
      </table>
    </div>
    <div class="col-10 product-detail">
      <div class="row mt-5">
        <div class="col-6 product-detail-image">
          <!--画像動作確認のため 17行目 コメントアウト / 18行目 でページの動作を確認-->
          <!--<#%= attachment_image_tag @product, :products_image, :fill, 100, 200, format: "jpeg", fallback: "no_image_kuma.jpg" %><br>-->
          <%= image_tag "no_image_kuma.jpg", width: "100%", height: "100%" %>
        </div>
        <div class="col-6 product-detail-text">
          <!--ここからカート追加関連：リンク先等は仮入れ中-->
          <%= form_with model:@cart_item, local:true do |f| %>
          <h3><%= @product.name %></h3>
          <p><%= @product.description %></p>
          <br>
          <br>
          <p><%= @product.base_price %>(税込) ※要税計算か？</p>
          <%= f.label :quantity, "購入数" %>
            <!--ここから課題：在庫が5個未満でも選択できてしまう-->
              <%= f.select :quantity, [1,2,3,4,5] %>
            <!--ここまで課題：在庫が5個未満でも選択できてしまう-->
          <%= f.submit "カートに入れる", class:"btn btn-sm btn-info" %>
          <% end %>
          <!--ここまでカート追加関連-->
        </div>
      </div>
    </div>
  </div>
</div>